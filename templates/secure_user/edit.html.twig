{% extends 'base.html.twig' %}

{% block title %}Edit SecureUser{% endblock %}

{% block body %}
    <h1>Edit SecureUser</h1>

    {{ include('secure_user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('secure_user_index') }}">back to list</a>

    {{ include('secure_user/_delete_form.html.twig') }}
{% endblock %}
